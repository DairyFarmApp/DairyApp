# Scripts

This directory is reserved for reviewed repository automation. Phase 1 does not add operational scripts because the required framework commands are documented directly and no repeated custom workflow yet justifies one.
